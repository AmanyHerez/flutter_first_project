import 'package:flutter/material.dart';
class ShopeScreen extends StatelessWidget {
  const ShopeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
          child: Text('Shope Screen'),


    );
  }
}
